#include <iostream>

using namespace std;

class Character {

protected:
    string name;
    int age;
    string gender;
    int HP;
    int Stamina;
    int level;

public:
    Character(string name, int age, string gender) : name(name), age(age), gender(gender) {}

    
};

int main() {

    return 0;
}